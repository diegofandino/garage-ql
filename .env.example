GRAPH_QL_ENDPOINT=/api/graphql
# Only used locally / as a fallback — on Vercel, lib/api-url.ts uses the
# auto-injected VERCEL_URL env var instead, so you don't need to set this
# per-deployment in the Vercel dashboard.
BASE_API_URL=http://localhost:3000